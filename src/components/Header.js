import React from "react";

const Header = () => {
  return (
    <header
      className="header"
      style={{ background: "orange", color: "white", padding: 10 }}
    >
      <h1>Keeper App</h1>
    </header>
  );
};

export default Header;
